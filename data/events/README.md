# Event Label Templates

This directory contains manual label templates used by downstream research runs.

`semis_guidance_surprise_labels.csv` is a header-only template for enriching the SEC Item 2.02 semiconductor earnings corpus with point-in-time expectation and surprise labels.

The template is intentionally separate from scraped SEC event provenance. Event occurrence and expectation surprise are different labels and should be reviewed separately.
