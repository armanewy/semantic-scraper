name: ecommerce
description: Default ecommerce extraction pack for product titles, prices, ratings, and availability.
policy: ranker-local
ranker: default
thresholds:
  min_confidence: 0.75
  min_margin: 0.15
  min_validator_confidence: 0.70
  min_ranker_confidence: 0.70
  min_ranker_margin: 0.00
  max_ranker_penalties: 1
  llm_fallback_policy: recoverable-only
validators: validators.yml
supported_fields: supported-fields.yml
model_card: model-card.md
